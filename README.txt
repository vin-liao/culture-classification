Make sure you have everything installed. If you are not sure the libraries required are installed, you can use the
requirement.txt given here.

To install all the dependencies in requirement.txt:
pip install -r /path/to/project/requirement.txt
